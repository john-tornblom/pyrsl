This is a language definition for text editors based on gtksourceview, e.g. 
gedit, mousepad and meld. To install the language definition, copy the file
rsl.lang into a gtksourceview language specification folder available in a XDG
data dir ($XDG_DATA_DIRS). For example:

$ mkdir -p ~/.local/share/gtksourceview-3.0/language-specs
$ cp rsl.lang ~/.local/share/gtksourceview-3.0/language-specs/rsl.lang
